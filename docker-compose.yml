services:
  boss-bot:
    build: .
    container_name: boss-bot
    restart: unless-stopped
    env_file:
      - .env
    working_dir: /app
    volumes:
      - .:/app
    command: ["sh", "-c", "while true; do python -m app.bot; sleep 2; done"]  # ← PUT IT HERE
